generated: '2026-09-13'
method: probed
source: https://caretta.so/.well-known/ai-catalog.json
description: 'AI Catalog manifest observed at /.well-known/ai-catalog.json on caretta.so. The AI Catalog
  Standard is the publishing half of Agentic Resource Discovery (ARD): a JSON manifest listing the agentic
  resources a domain offers — MCP servers, agent cards, skills, APIs — each typed by media type and identified
  by a domain-anchored urn:air: URN. Two verdicts are recorded: a structural grade against the data model,
  and the result of the ARD project''s own conformance CLI run against this exact body. Provider-published
  by construction — served from the provider''s own host; nothing here is derived or generated.'
discovery:
  path: /.well-known/ai-catalog.json
  canonical: false
  predecessor_only: true
  attempts:
  - path: /.well-known/ard.json
    http_status: 404
  - path: /.well-known/ai-catalog.json
    http_status: 200
  host: caretta.so
conformance:
  spec: AI Catalog 1.0 / ARD v0.91
  grade: near-conformant
  spec_version_declared: '1.0'
  hard_failures: []
  soft_gaps:
  - content-type-not-ai-catalog-json
  - entry-no-representativeQueries
  - host-identifier-not-did-web
  official_tool:
    tool: ARD conformance-test v0.9.1 (ards-project/ard-spec)
    status: FAIL
    critical_errors: 1
    errors:
    - '[Caretta MCP server] ''trustManifest'' is missing required ''identity'' field.'
    warnings:
    - '[Caretta MCP server] No ''representativeQueries''. The semantic index is built from this term,
      so the entry will not be found by search — it is a valid catalog entry but not a discoverable ARD
      entry.'
    - '[Caretta REST API] No ''representativeQueries''. The semantic index is built from this term, so
      the entry will not be found by search — it is a valid catalog entry but not a discoverable ARD entry.'
    - '[Caretta llms.txt] Media type ''text/markdown'' is based on standard discovery type ''text/markdown''
      but has missing required parameters: profile="urn:air:agent-skills".'
    - '[Caretta llms.txt] No ''representativeQueries''. The semantic index is built from this term, so
      the entry will not be found by search — it is a valid catalog entry but not a discoverable ARD entry.'
    - '[Caretta agent skills index] No ''representativeQueries''. The semantic index is built from this
      term, so the entry will not be found by search — it is a valid catalog entry but not a discoverable
      ARD entry.'
    - '[Caretta authentication guide] Media type ''text/markdown'' is based on standard discovery type
      ''text/markdown'' but has missing required parameters: profile="urn:air:agent-skills".'
    - '[Caretta authentication guide] No ''representativeQueries''. The semantic index is built from this
      term, so the entry will not be found by search — it is a valid catalog entry but not a discoverable
      ARD entry.'
    informational:
    - '[Caretta REST API] Media type ''application/vnd.oai.openapi+json'' is a valid application extension
      media type. ARD permits extension types without core registration.'
    - '[Caretta agent skills index] Media type ''application/json'' is a valid application extension media
      type. ARD permits extension types without core registration.'
  did_web: null
catalog:
  host_display_name: Caretta
  host_identifier: caretta.so
  entry_count: 5
  types:
    application/json: 1
    application/mcp-server-card+json: 1
    application/vnd.oai.openapi+json: 1
    text/markdown: 2
  representative_queries: 0
  trust_manifests: 1
  file: caretta-ai-catalog.json
transport:
  http_status: 200
  content_type: application/json
  access_control_allow_origin: '*'
x-evidence:
  fetched: '2026-09-13'
  url: https://caretta.so/.well-known/ai-catalog.json
  http_status: 200
  content_type: application/json
  note: Verbatim body saved alongside this manifest. No field inferred.
