generated: '2026-08-13'
method: probed
source: https://www.caretta.so/docs/.well-known/agent-card.json

discovery:
  path: /docs/.well-known/agent-card.json
  canonical: false
  host: www.caretta.so
  note: >-
    Not served at the RFC 8615 origin root. https://www.caretta.so/.well-known/agent-card.json
    and /.well-known/agent.json both return 404, as do the same paths on
    gateway.caretta.app. The card is served from the Mintlify-hosted docs
    subpath and is formally advertised by the docs origin itself through an
    HTTP Link header on https://www.caretta.so/docs:
    `link: </docs/.well-known/agent-card.json>; rel="agent-card"`.
    It is therefore a genuinely published, discoverable document on a host
    Caretta controls, but an agent probing only the origin root will miss it.

conformance:
  spec: A2A 1.0.0
  grade: near-conformant
  protocol_version: '0.3'
  preferred_transport: HTTP+JSON
  hard_checks:
    capabilities_is_object: true
    protocol_version_present: true
    skills_is_array: true
  deviations:
  - supportedInterfaces-instead-of-additionalInterfaces
  - protocol-version-0.3-pre-1.0
  - not-served-at-origin-well-known-root
  deviation_note: >-
    All three A2A 1.0.0 hard checks pass, so this is not a flavored card, but it
    carries pre-1.0 field naming (supportedInterfaces rather than
    additionalInterfaces) and declares protocolVersion 0.3 rather than a 1.0
    line. Graded near-conformant rather than conformant on that basis.

card:
  name: Caretta Docs
  url: https://www.caretta.so/docs/
  version: 1.0.0
  skills: 1
  file: caretta-agent-card.json

x-scope-warning: >-
  This card describes Caretta's DOCUMENTATION site, not the Caretta product
  agent. name is "Caretta Docs", url and provider.url both point at
  https://www.caretta.so/docs/, and its single skill is the published
  documentation skill. It is generated by the Mintlify docs platform. The
  actual agent-callable Caretta surface is the MCP server at
  https://gateway.caretta.app/mcp (see mcp/caretta-mcp.yml), which the card
  does not reference. Recorded as found; not to be read as an A2A interface
  onto Caretta's call data.

x-evidence:
  fetched: '2026-08-13'
  url: https://www.caretta.so/docs/.well-known/agent-card.json
  http_status: 200
  content_type: application/json
  negative_probes:
  - {url: 'https://www.caretta.so/.well-known/agent-card.json', http_status: 404}
  - {url: 'https://www.caretta.so/.well-known/agent.json', http_status: 404}
  - {url: 'https://gateway.caretta.app/.well-known/agent-card.json', http_status: 404}
  - {url: 'https://gateway.caretta.app/.well-known/agent.json', http_status: 404}
